function encode(longUrl: string): string {
  // encode    -> Time { O(1), θ(1), Ω(1) } and Space { O(1), θ(1), Ω(1) }
  return longUrl;
}

function decode(shortUrl: string): string {
  // decode    -> Time { O(1), θ(1), Ω(1) } and Space { O(1), θ(1), Ω(1) }
  return shortUrl;
}
