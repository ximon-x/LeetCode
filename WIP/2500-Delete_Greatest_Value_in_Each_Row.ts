function deleteGreatestValue(grid: number[][]): number {
  let answer = 0;

  while (grid[0].length !== 0) {
    let maxNums: number[] = new Array(grid.length, 0);

    for (let i = 0; i < grid[0].length; i++) {
      if (maxNums[0]) {
      }
    }
  }

  return answer;
}
